config:
  target: "http://localhost:5000"
  phases:
    - duration: 60 # Run the test for 60 seconds
      arrivalRate: 10 # Start with 10 requests per second
      rampTo: 50 # Gradually increase to 50 req/sec
scenarios:
  - flow:
      - get:
          url: "/api/transactions"
